import { NotificationHostDirective } from './notification-host.directive';

describe('NotificationHostDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationHostDirective();
    expect(directive).toBeTruthy();
  });
});
